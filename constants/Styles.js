import React from 'react'
import {
    StyleSheet,
} from 'react-native'

import Colors from '../constants/Colors'

export default styles = StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: '#fff',
    }
})